# Nucleus observations — Derivative Action Tower

The live evidence ledger of this orbital client, per the discipline of
[FRACTAL-GRAPH-ARCHITECTURE.md](../../FRACTAL-GRAPH-ARCHITECTURE.md):

\[
\boxed{\text{observation}\neq\text{immediate refactor}}
\]

One entry per rung. Entries record evidence; they do not prescribe
nucleus changes.

---

## OBSERVATION DA-0

```text
tower:                Derivative Action
gate / level:         A / 0
contextual radius:    0 (carriers in isolation)

symptom / fact:       a derivative application required no new Level-0
                      ontology: V={x,y,u,z}, O={product,sum},
                      P={in1,in2,out} are ordinary counted_sets

exact caller:         level-0-carrier/test.f90

mathematical concept: symbolic member domains

local necessity:      yes — every later rung stands on these carriers
                      evidence: all Gate-A tests construct them first
global necessity:     yes — third tower using the identical ontology
                      evidence: calculator, learning, derivative all
                      begin with counted_set and nothing else

graph role:           none yet — no graph exists at this rung

comparison:           identical to Calculator L0 and Learning L0 in
                      kind; only cardinalities differ

suspected nucleus implication: carrier layer is stable across orbital
                      families

confidence:           high
action:               observe
```

---

## OBSERVATION DA-1

```text
tower:                Derivative Action
gate / level:         A / 1
contextual radius:    0

symptom / fact:       derivative potential is completely latent:
                      R_flow ⊆ O×V×P holds computation structure, not
                      derivative metadata. The only derivative-relevant
                      fact — y consumed twice, x once — is plain
                      ternary membership.

exact caller:         level-1-relation/test.f90 (check_fan_out)

mathematical concept: typed finite-arity relation

local necessity:      yes — the six tuples are the specimen's one
                      structural source of truth
global necessity:     yes — same relation shape as both prior towers
                      evidence: stored_relation with [O,V,P] signature,
                      set semantics, unchanged

graph role:           not applicable yet

comparison:           learning's R_flow also carried a fan-out (yhat
                      produced then consumed); here the fan-out sits on
                      an INPUT, which is what makes reverse
                      accumulation eventually interesting

suspected nucleus implication: none — the relation layer needed nothing

confidence:           high
action:               observe
```

---

## OBSERVATION DA-2

```text
tower:                Derivative Action
gate / level:         A / 2
contextual radius:    0

symptom / fact:       operation dependency D={(product,sum)} derived by
                      restrict/project/compose exactly as the other
                      towers derive theirs; nothing derivative-specific
                      appeared in the algebra

exact caller:         level-2-relation-algebra/test.f90

mathematical concept: relation algebra (restriction, projection, binary
                      composition)

local necessity:      yes — execution interpretation (L4) and any
                      future evaluation need D
global necessity:     yes — third independent client of the same three
                      primitives; natural join still unearned by anyone

graph role:           none yet

comparison:           identical derivation road to Calculator L2 and
                      Learning L2

suspected nucleus implication: the three earned algebra primitives
                      remain sufficient at this radius

confidence:           high
action:               observe
```

---

## OBSERVATION DA-3

```text
tower:                Derivative Action
gate / level:         A / 3
contextual radius:    0–1 (object in its owning context)

symptom / fact:       relational_graph contributed structural closure
                      and identity — owned carriers, owned relations,
                      signature closure, twin-rejection — and nothing
                      else was asked of it. No tangent seat, no adjoint
                      seat, no derivative metadata.

exact caller:         level-3-graph/test.f90

mathematical concept: G = (S, R) ownership

local necessity:      partially — Gate A's L4/L6 views borrow
                      graph-OWNED relations, so ownership is the
                      substrate the interpretations stand on
global necessity:     unknown — whether derivative EVALUATION (Gate B)
                      needs the graph as context/environment rather
                      than closure is exactly the open fractal question
                      evidence: none yet at this radius

graph role:           owner (structural closure); not an operand;
                      context role untested at this gate

comparison:           same contribution as in Learning L3; Learning L9
                      later used ownership for the statement's lifetime
                      proof — Gate C may echo that

suspected nucleus implication: none yet; watch at Gate B

confidence:           medium
action:               test at larger radius (Gate B)
```

---

## OBSERVATION DA-4

```text
tower:                Derivative Action
gate / level:         A / 4
contextual radius:    0–1

symptom / fact:       three concepts remained three:
                      dependency relation ≠ directed interpretation
                      ≠ derivative traversal. The topological walk
                      [product, sum] is computation order; no tangent
                      rides it, no cotangent rides it backward.

exact caller:         level-4-graph-calculus/test.f90

mathematical concept: explicit graph interpretation of a relation

local necessity:      yes — future evaluation order must come from
                      structure, and this is where it comes from
global necessity:     yes for the first two concepts (three towers);
                      the third (derivative traversal) exists nowhere
                      yet and MUST NOT be conflated with this walk
                      evidence: Gate A contains no traversal that
                      carries derivative quantities

graph role:           the view borrows a graph-owned relation — the
                      established borrowing contract, unchanged

comparison:           identical mechanism to Learning L4; the
                      historical derivative-walks plan conflated
                      "walk" with "derivative walk" — the tower keeps
                      them separate until Gate B earns a connection

suspected nucleus implication: if Gate B adds derivative traversal, it
                      should be a NEW interpretation/act, not a
                      mutation of the execution walk

confidence:           high
action:               observe; revisit at Gate B
```

---

## OBSERVATION DA-5

```text
tower:                Derivative Action
gate / level:         A / 5
contextual radius:    0

symptom / fact:       primal state used the ordinary field on ordinary
                      subdomains: X={y,x} with q_X=[3,2] by
                      enumeration, C={u,z} with NO field. No
                      tangent_field, adjoint_field, dual_field, or
                      seed type was needed or invented.

exact caller:         level-5-field-calculus/test.f90

mathematical concept: fields on member domains; roles by subdomain

local necessity:      yes — the base point must exist before Gate B
                      can linearize anything at it
global necessity:     unknown for derivative VECTORS: Gate B will
                      reveal whether a tangent/cotangent needs its own
                      type or is simply another field on X (or on a
                      dual-role domain)
                      evidence: none yet — deliberately

graph role:           none — a field needs a domain, not a graph
                      (confirmed a third time)

comparison:           exact echo of Learning L5's discovery: roles are
                      domains, not field subclasses; uncomputed slots
                      own no numbers

suspected nucleus implication: derivative vectors may also be plain
                      fields distinguished by domain/role — but that
                      is a hypothesis for Gate B, not a Gate-A fact

confidence:           high (for Gate A's claims); low (for the Gate-B
                      hypothesis)
action:               observe; test at Gate B
```

---

## OBSERVATION DA-6A

```text
tower:                Derivative Action
gate / level:         A / 6
contextual radius:    0–1

symptom / fact:       the structural Jacobian pattern
                      J_ZX = {(z,x),(z,y)} was derived — reachability
                      of inputs to the response over the interpreted
                      value dependency A_V — before any derivative
                      arithmetic exists anywhere

exact caller:         level-6-derivative-structure/test.f90
                      (derive_jacobian)

mathematical concept: structural sparsity precedes derivative values

local necessity:      yes — Gate B's actions will live inside this
                      support
global necessity:     yes — independently confirms Learning L6's
                      J_Theta observation with a second, richer
                      specimen

graph role:           the dependency view borrows a graph-owned A_V

comparison:           Learning derived J_Theta={(r,w)} the same way;
                      this tower's J has two columns and a fan-out
                      behind one of them

suspected nucleus implication: structural-Jacobian derivation is now a
                      recurring seam (two towers); still test-local,
                      correctly — promotion unearned

confidence:           high
action:               observe
```

---

## OBSERVATION DA-6B

```text
tower:                Derivative Action
gate / level:         A / 6
contextual radius:    0–1

symptom / fact:       the reverse structure J_XZ = {(x,z),(y,z)} is
                      transpose_of(J_ZX): a borrowing view,
                      materialized() = false; no second stored reverse
                      pattern, no backprop graph

exact caller:         level-6-derivative-structure/test.f90
                      (check_reverse_is_the_view)

mathematical concept: structural transpose duality

local necessity:      yes — reverse support must be readable
global necessity:     yes — third tower using transpose-as-view
                      (calculator L6, learning L6, derivative L6):
                      forward/reverse structural dependency is ONE
                      truth, everywhere so far

graph role:           view over a materialized citizen

comparison:           identical to both prior towers

suspected nucleus implication: the transpose-view contract is one of
                      the most-confirmed nucleus behaviors; Gate B's
                      reverse action should respect (not duplicate) it

confidence:           high
action:               observe
```

---

## OBSERVATION DA-6C

```text
tower:                Derivative Action
gate / level:         A / 6
contextual radius:    0–1

symptom / fact:       SUPPORT IS NOT MULTIPLICITY. J_ZX holds (z,y)
                      exactly once — a relation is a set — while the
                      value dependency A_V holds TWO routes by which y
                      influences z: (y,z) direct and (y,u),(u,z)
                      through the product. The Jacobian pattern
                      collapses path multiplicity by construction.

exact caller:         level-6-derivative-structure/test.f90
                      (check_support_is_not_multiplicity)

mathematical concept: sparsity support vs contribution structure

local necessity:      yes — this is the specimen's reason to exist
global necessity:     suspected yes, and NEW — neither prior tower
                      could see this (learning's J had one path per
                      entry)

graph role:           the finer structure (A_V / R_flow) is graph-owned;
                      the collapsed pattern is derived

comparison:           first tower to expose it

suspected nucleus implication: numerical reverse accumulation (Gate B)
                      must operate over the computational
                      dependency/evaluation structure — A_V, or R_flow
                      itself — NOT merely scan the final J-pattern and
                      infer contributions. Support says WHO matters;
                      the evaluation structure says WHY and HOW OFTEN.
                      This likely shapes the entire Gate-B design.

confidence:           high (the fact); medium (its design consequence)
action:               candidate driver for Gate-B design review
```

---

## OBSERVATION DA-6D

```text
tower:                Derivative Action
gate / level:         A / 6
contextual radius:    0–1

symptom / fact:       no location relation was needed. The response
                      Z={z} is a subdomain chain Z ↪ C ↪ V — the
                      response already IS a value slot. Learning
                      needed Y + L={(r,e)} because residual ROWS were
                      not value slots.

exact caller:         level-6-derivative-structure/test.f90
                      (check_response_domain)

mathematical concept: subobject embedding vs location relation

local necessity:      L is locally UNNECESSARY here
global necessity:     L remains globally NECESSARY where the answering
                      carrier is distinct from the value carrier
                      (learning's residual rows; any future
                      row-indexed system)

graph role:           not applicable

comparison:           the towers disagree, and the disagreement is the
                      evidence: L is CONTEXTUAL, not universal — an
                      instance of the independent-axes law (local
                      unnecessity does not imply global unnecessity,
                      and vice versa)

suspected nucleus implication: never bake a location relation into a
                      universal residual/response contract; let each
                      statement choose subdomain embedding or location
                      relation as its carriers demand

confidence:           high
action:               observe
```

---

## OBSERVATION DA-6E

```text
tower:                Derivative Action
gate / level:         A / 6 (gate boundary)
contextual radius:    0–1

symptom / fact:       Gate A reached complete derivative structure
                      without minimization appearing at all. No
                      solver, no host, no attach, no square contract
                      was touched by any Gate-A rung.

exact caller:         the whole gate; check_imports.sh forbids
                      graph_minimization and class_graph_gmres
                      everywhere

mathematical concept: (absence of) the upper-nucleus crossing

local necessity:      minimization is locally unnecessary at Gate A
global necessity:     UNKNOWN — deliberately unresolved. This does NOT
                      conclude "Derivative Action does not need Level
                      7": how this orbit crosses the upper nucleus
                      levels (minimization / constitution / statement)
                      is exactly the Gate-A review question.

graph role:           untested at this radius

comparison:           calculator and learning both crossed L7 with
                      supplied oracles before constituting; whether
                      derivative action follows that shape (a supplied
                      derivative oracle before derivative
                      constitution?) is open

suspected nucleus implication: none yet

confidence:           high (the fact); none offered (the design)
action:               Gate-A review decides the Gate-B shape
```
