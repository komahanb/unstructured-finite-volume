include ../Makefile.in

#=====================================================================#
# Object Files
#=====================================================================#

include OBJECTS

#=====================================================================#
# Library
#=====================================================================#

default: $(objects) 
	ar rvs libufvm.a $(objects)
	@echo "----------- libufvm.a created -----------"

install:
	mv *.o ../lib
	mv *.a ../lib
	mv *.mod ../lib

clean:
	@rm -f ../lib/*.o ../lib/*.mod ../lib/*.so ../lib/*.a

#=====================================================================#
# Rule
#=====================================================================#

.SUFFIXES: .f90 .o
.PHONY: all clean 

%.o: %.f90
	$(F90) $(F90FLAGS) -c $<
